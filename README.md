# JustGo Platform

Monorepo for the JustGo platform.

## Structure
- `backend-core`: FastAPI backend service.
- `edge-service`: Placeholder for edge service.

## Setup
1. Create a `.env` file based on the example.
2. Run `docker-compose up --build`.
